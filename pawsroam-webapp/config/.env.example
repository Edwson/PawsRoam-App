# PawsRoam Environment Variables Example
# Copy this file to .env in the SAME directory (pawsroam-webapp/config/) and fill in your actual values.
# IMPORTANT: DO NOT commit your actual .env file to version control if it contains sensitive credentials.
# The .gitignore file in the project root should ideally list 'config/.env'.

# --- Database Configuration ---
DB_HOST="localhost"
DB_NAME="pawsroam_db"
DB_USERNAME="root"
DB_PASSWORD=""
DB_PORT="3306" # Optional, if not default

# --- API Keys ---
# Get your Google Maps API Key from Google Cloud Console:
# https://console.cloud.google.com/google/maps-apis/overview
GOOGLE_MAPS_API_KEY=""

# Get your Google Gemini AI API Key from Google AI Studio:
# https://aistudio.google.com/app/apikey
GEMINI_AI_API_KEY=""

# --- Application Settings ---
APP_ENV="development" # development, staging, production
APP_DEBUG="true" # true for development, false for production
APP_URL="http://localhost:8000" # Your local development URL
DEFAULT_TIMEZONE="UTC" # Example: "America/New_York", "Europe/London", "Asia/Tokyo"
DEFAULT_LANGUAGE="en" # Default language code for the application

# --- Session & Security ---
SESSION_LIFETIME="1440" # Session lifetime in minutes (e.g., 24 hours)
CSRF_TOKEN_NAME="csrf_token" # Name for CSRF token in forms/session

# --- Email Configuration (Example for future use) ---
# MAIL_MAILER="smtp"
# MAIL_HOST="smtp.mailtrap.io"
# MAIL_PORT="2525"
# MAIL_USERNAME=""
# MAIL_PASSWORD=""
# MAIL_ENCRYPTION="tls"
# MAIL_FROM_ADDRESS="noreply@pawsroam.com"
# MAIL_FROM_NAME="PawsRoam"

# --- Other external service keys ---
# STRIPE_PUBLISHABLE_KEY=""
# STRIPE_SECRET_KEY=""
